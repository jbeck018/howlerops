# =============================================================================
# Automatic Semantic Versioning Workflow
# =============================================================================
# This workflow automatically creates version tags based on conventional commits
# when changes are pushed to the main branch.
#
# Conventional Commit Format:
#   feat: New feature (triggers MINOR version bump: 0.1.0 -> 0.2.0)
#   fix: Bug fix (triggers PATCH version bump: 0.1.0 -> 0.1.1)
#   BREAKING CHANGE: Breaking change (triggers MAJOR version bump: 0.1.0 -> 1.0.0)
#   chore:, docs:, style:, refactor:, test: (NO version bump)
#
# How it works:
# 1. Analyzes commits since last tag
# 2. Determines next version based on commit types
# 3. Creates and pushes a new tag (e.g., v0.2.0)
# 4. The tag push triggers release.yml and release-macos.yml workflows
#
# IMPORTANT: This workflow uses RELEASE_PAT (Personal Access Token) instead of
# GITHUB_TOKEN because tags/releases created with GITHUB_TOKEN cannot trigger
# other workflows. To enable automatic release builds:
# 1. Create a PAT with 'repo' scope at https://github.com/settings/tokens
# 2. Add it as a repository secret named RELEASE_PAT
# 3. If RELEASE_PAT is not configured, falls back to GITHUB_TOKEN (releases won't auto-trigger)
#
# Example commit messages:
#   feat: Add dark mode support
#   fix: Resolve memory leak in connection pool
#   feat!: Redesign API endpoints (! indicates breaking change)
#   chore: Update dependencies
# =============================================================================

name: Auto Version

on:
  push:
    branches:
      - main
    # Don't run on tag pushes (prevents infinite loop)
    tags-ignore:
      - '**'
    # Only run when there are actual code changes
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.github/workflows/auto-version.yml'

# Required permissions
permissions:
  contents: write  # Needed to create and push tags

jobs:
  tag:
    name: Create Version Tag
    runs-on: ubuntu-latest

    # Skip if commit message contains [skip-version] or [no-version]
    if: "!contains(github.event.head_commit.message, '[skip-version]') && !contains(github.event.head_commit.message, '[no-version]')"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for proper version calculation
          # Use RELEASE_PAT if available, otherwise fall back to GITHUB_TOKEN
          # RELEASE_PAT is required for the tag push to trigger release workflows
          token: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}

      - name: Get latest tag
        id: get_latest_tag
        run: |
          # Get the latest tag, default to v0.0.0 if none exist
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "Latest tag: $LATEST_TAG"

      - name: Determine version bump
        id: version_bump
        run: |
          LATEST_TAG="${{ steps.get_latest_tag.outputs.latest_tag }}"

          # Get commits since last tag
          if [ "$LATEST_TAG" = "v0.0.0" ]; then
            COMMITS=$(git log --format=%s)
          else
            COMMITS=$(git log ${LATEST_TAG}..HEAD --format=%s)
          fi

          echo "Analyzing commits:"
          echo "$COMMITS"

          # Check for breaking changes (MAJOR bump)
          if echo "$COMMITS" | grep -qE '^[a-z]+(\(.+\))?!:|^BREAKING CHANGE:'; then
            BUMP_TYPE="major"
            echo "Found breaking change - MAJOR version bump"
          # Check for features (MINOR bump)
          elif echo "$COMMITS" | grep -qE '^feat(\(.+\))?:'; then
            BUMP_TYPE="minor"
            echo "Found feature - MINOR version bump"
          # Check for fixes (PATCH bump)
          elif echo "$COMMITS" | grep -qE '^fix(\(.+\))?:'; then
            BUMP_TYPE="patch"
            echo "Found fix - PATCH version bump"
          else
            BUMP_TYPE="none"
            echo "No version-bumping commits found (only chore/docs/etc)"
          fi

          echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT

      - name: Calculate new version
        id: new_version
        if: steps.version_bump.outputs.bump_type != 'none'
        run: |
          LATEST_TAG="${{ steps.get_latest_tag.outputs.latest_tag }}"
          BUMP_TYPE="${{ steps.version_bump.outputs.bump_type }}"

          # Strip any leading 'v' characters for calculation. Using `#v` only
          # removes one, so a previously mis-generated tag like vv0.14.1 would
          # leave "v0.14.1" and make MAJOR="v0", regenerating vv0.14.2 forever.
          VERSION=$(printf '%s' "$LATEST_TAG" | sed -E 's/^v+//')

          # Split version into components
          MAJOR=$(echo $VERSION | cut -d. -f1)
          MINOR=$(echo $VERSION | cut -d. -f2)
          PATCH=$(echo $VERSION | cut -d. -f3)

          # Bump version based on type
          if [ "$BUMP_TYPE" = "major" ]; then
            MAJOR=$((MAJOR + 1))
            MINOR=0
            PATCH=0
          elif [ "$BUMP_TYPE" = "minor" ]; then
            MINOR=$((MINOR + 1))
            PATCH=0
          elif [ "$BUMP_TYPE" = "patch" ]; then
            PATCH=$((PATCH + 1))
          fi

          NEW_VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version will be: $NEW_VERSION"

      - name: Check PAT availability
        id: check_pat
        run: |
          if [ -n "${{ secrets.RELEASE_PAT }}" ]; then
            echo "has_pat=true" >> $GITHUB_OUTPUT
            echo "✅ RELEASE_PAT is configured - releases will auto-trigger"
          else
            echo "has_pat=false" >> $GITHUB_OUTPUT
            echo "⚠️ RELEASE_PAT not configured - using GITHUB_TOKEN (releases won't auto-trigger)"
            echo "To enable auto-triggered releases, add RELEASE_PAT secret with 'repo' scope"
          fi

      - name: Create and push tag
        if: steps.version_bump.outputs.bump_type != 'none'
        env:
          NEW_VERSION: ${{ steps.new_version.outputs.new_version }}
          # Use PAT for push to trigger downstream workflows
          GH_TOKEN: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}
        run: |
          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create annotated tag
          git tag -a "$NEW_VERSION" -m "chore(release): $NEW_VERSION

          Automatically generated release tag based on conventional commits.

          Generated by: ${{ github.workflow }}
          Commit: ${{ github.sha }}"

          # Push the tag (uses the token from checkout step)
          git push origin "$NEW_VERSION"

          echo "✅ Created and pushed tag: $NEW_VERSION"
          if [ "${{ steps.check_pat.outputs.has_pat }}" = "true" ]; then
            echo "🚀 This will trigger the release workflows (release.yml and release-macos.yml)"
          else
            echo "⚠️ Release workflows will NOT auto-trigger (RELEASE_PAT not configured)"
            echo "   Manually trigger releases at: ${{ github.server_url }}/${{ github.repository }}/actions"
          fi

      - name: Summary
        if: always()
        run: |
          echo "### Auto-Versioning Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.version_bump.outputs.bump_type }}" = "none" ]; then
            echo "**No version bump required**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Commits did not contain feat:, fix:, or BREAKING CHANGE:" >> $GITHUB_STEP_SUMMARY
          else
            echo "**New version created:** \`${{ steps.new_version.outputs.new_version }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- Previous: \`${{ steps.get_latest_tag.outputs.latest_tag }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- Bump type: **${{ steps.version_bump.outputs.bump_type }}**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            if [ "${{ steps.check_pat.outputs.has_pat }}" = "true" ]; then
              echo "✅ **Release workflows will auto-trigger** (RELEASE_PAT configured)" >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️ **Release workflows will NOT auto-trigger**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "To enable automatic releases, add a \`RELEASE_PAT\` secret with \`repo\` scope." >> $GITHUB_STEP_SUMMARY
              echo "[Create PAT](https://github.com/settings/tokens) | [Add Secret](/${{ github.repository }}/settings/secrets/actions)" >> $GITHUB_STEP_SUMMARY
            fi
          fi

# =============================================================================
# Usage Guide
# =============================================================================
#
# Conventional Commit Examples:
#
# PATCH version bump (0.1.0 -> 0.1.1):
#   git commit -m "fix: resolve database connection timeout"
#
# MINOR version bump (0.1.0 -> 0.2.0):
#   git commit -m "feat: add user authentication"
#
# MAJOR version bump (0.1.0 -> 1.0.0):
#   git commit -m "feat!: redesign API structure"
#   or
#   git commit -m "feat: redesign API
#
#   BREAKING CHANGE: endpoints now use /v2/ prefix"
#
# No version bump:
#   git commit -m "chore: update dependencies"
#   git commit -m "docs: improve README"
#   git commit -m "test: add integration tests"
#
# Skip versioning for a specific commit:
#   git commit -m "feat: new feature [skip-version]"
#
# =============================================================================
