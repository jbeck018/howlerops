# Claude Flow runtime files
data/
logs/
sessions/
neural/
*.log
*.tmp

# Daemon runtime state (rewritten continuously by the running daemon)
daemon.pid
daemon-state.json

# Generated metrics snapshots (regenerated by the daemon each run)
metrics/codebase-map.json
metrics/security-audit.json
